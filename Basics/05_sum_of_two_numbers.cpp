#include <iostream>
using namespace std;

int main() {
    int a, b;
    
    cout << "Enter two numbers (separated by space):" << endl;
    cin >> a >> b;

    cout << "Sum of " << a << " and " << b << " is: " << (a + b) << endl;

    return 0;
}
